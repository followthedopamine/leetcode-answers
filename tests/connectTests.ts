import TreeNode from "../populating-next-right-pointers-in-each-node/TreeNode";
import assert from "assert";
import connect from "../populating-next-right-pointers-in-each-node/connect";

// TODO: Finish

// describe("connect()", () => {
//   it("Should populate each next pointer to point to its next right node. If there is no next right node, the next pointer should be set to NULL", () => {
//     const problem = new TreeNode(
//       1,
//       new TreeNode(2, new TreeNode(4), new TreeNode(5)),
//       new TreeNode(3, new TreeNode(6), new TreeNode(7))
//     );
//     const answer = new TreeNode(1, undefined, new TreeNode(2));
//     assert.deepEqual(connect(problem), answer);
//   });
// });
